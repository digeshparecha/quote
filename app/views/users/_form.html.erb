<%= simple_form_for [current_user], html: { class: "form line-item-date" } do |f| %>
  <%= form_error_notification(current_user) %>
  <% User::PROFILE_FIELDS.each do |field| %>
    <%= render "users/#{field}", f: f %>
  <% end %>
  <%= f.submit class: "btn btn-success" %>
<% end %>